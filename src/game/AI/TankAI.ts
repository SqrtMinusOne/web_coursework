import {Tank} from "../entities/Tank";
import {Entity} from "../entities/Entity";

export class tankAI{
    private tank: Tank;

    constructor (tank){
        this.tank = tank;
    }

    makeDecision(){

    }

    attackEnemy(enemy: Entity){

    }
}